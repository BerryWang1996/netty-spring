# Installed by `npm install` for smoke.js (the ws client) — never committed.
node_modules/
# Regenerated by `npm install` (CI uses install, not ci); the resolved registry URLs are
# environment-specific, so the lockfile is not committed.
package-lock.json
