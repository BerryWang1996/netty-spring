# RC19 — 2.0.0 Prep Docs Plan

> 2 parallel implementer tracks (P + M) → 1 sequential track (G; depends on M) → controller-side cut.

**Goal:** Ship 3 forward-looking 2.0.0 design artifacts + release-notes §㉓ as `1.9.0-RC19`.

**Spec:** `docs/superpowers/specs/2026-06-07-rc19-2.0.0-prep-docs.md`.

**Branch:** `feature/1.9.0-rc19-2.0.0-prep-docs` (off RC18 `c8b3a21`).

---

## File Structure

| Path | Action |
|---|---|
| `docs/2.0.0/sharded-pubsub-feasibility.md` | **Create** (Track P) |
| `docs/2.0.0/boot3-compatibility-matrix.md` | **Create** (Track M) |
| `docs/2.0.0/1.9.0-to-2.0.0-migration-guide.md` | **Create** (Track G, after M) |
| `docs/release-notes-1.9.0.md` | Modify (header + §㉓) |
| 11 POMs | Modify (RC18 → RC19) |

---

## Track P — Sharded Pub/Sub Feasibility Study

**File:** `docs/2.0.0/sharded-pubsub-feasibility.md`

- [ ] **Step 1:** Read these references:
  - `docs/cluster-design.md` §Capacity / §深度瓶颈 / "M·(f·N-1)" formula
  - `docs/release-notes-1.9.0.md` §⑪ (RC7 Redis Cluster client; "sharded pub/sub → 2.0.0")
  - `netty-spring-websocket-cluster/.../cluster/redis/RedisClusterModePubSubBroker.java` (the regular cluster pub/sub broker shipped in RC7)
  - `netty-spring-websocket-cluster/.../cluster/redis/RedisPubSubBroker.java` (standalone reference)

- [ ] **Step 2:** Write the feasibility study following spec §2.1's required sections:
  - **Motivation**: cite the M·(f·N-1) capacity wall verbatim from cluster-design.md
  - **Why deferred from 1.9.0**: Lettuce 6.1.10 (Boot 2.7.18) vs Lettuce 6.2+ (Boot 3.x); Redis 7.0+ for SSUBSCRIBE/SPUBLISH
  - **API surface**: include Lettuce 6.2's relevant method signatures (StatefulRedisClusterPubSubConnection, RedisClusterPubSubAsyncCommands.ssubscribe / .spublish, sharded-channel pattern). Mark any API recall as "verify during 2.0.0 implementation".
  - **Fan-out reduction model**: table with f=1 (default), k=3/6/12 cluster masters, N=100/500/1000 sessions; original M·(f·N-1) vs sharded M·(f·N-1)/k formula; show approximate message-rate reduction
  - **Auto-config matrix entry sketch**: a new `pubsub-mode` property (regular/sharded, default regular), Lettuce-6.2 classpath gate, mutual-exclusion with single-node Redis (sharded requires `cluster-nodes`)
  - **Mutual-exclusion**: registry layout unchanged; only the cross-node fan-out broker differs
  - **Test strategy**: 6-master Testcontainers Redis Cluster (vs RC7's single-node), shard-distribution-verification test, fan-out reduction quantified
  - **Migration impact**: opt-in property; RC7 deployments default to regular cluster pub/sub
  - **Open questions** (the "to be answered during 2.0.0" list): explicit list — channel pattern matching limits, sharded multi-key SUBSCRIBE behavior, failover-during-resharding edge cases

- [ ] **Step 3:** Length target: 600-1000 words. Substantive but not exhaustive — this is a design study, not the design itself.

- [ ] **Step 4:** Commit: `git commit -m "docs(2.0.0): sharded pub/sub feasibility study (Track P)"`

---

## Track M — Boot 3.x Compatibility Matrix

**File:** `docs/2.0.0/boot3-compatibility-matrix.md`

- [ ] **Step 1:** Read references:
  - `pom.xml` (root) for the current Boot version + Spring + dependency versions
  - 11 module POMs (just version info)
  - `cluster-design.md` (to understand the deferred items list)

- [ ] **Step 2:** Mechanical surface analysis:
  ```bash
  grep -r "javax\\." --include="*.java" --include="*.xml" netty-spring-* netty-web*-* | grep -v "/target/" | awk -F: '{print $1}' | sort | uniq -c | sort -rn
  ```
  Capture per-module file counts of `javax.` imports. Categorize: `javax.servlet`, `javax.annotation`, `javax.persistence`, others. The Servlet ones are the bulk of jakarta migration; others may already be jakarta-compatible.

  Also check: `grep -r "@PostConstruct\|@PreDestroy" --include="*.java" netty-spring-* netty-web*-*` (these have known javax→jakarta moves).

- [ ] **Step 3:** Write the matrix doc per spec §2.2:
  - **JDK baseline** section (no change — JDK 17 is already the project minimum)
  - **Spring Boot** table (current 2.7.18 → target Boot 3.2.x or current LTS at 2.0.0 time; cite Spring Boot release support window)
  - **Spring Framework** subsection (javax→jakarta, removed APIs)
  - **Per-module impact table** with the columns from spec §2.2 (module name, javax import count, jakarta-impacted files, Spring API surface impact, dependency-version changes, expected effort xs/s/m/l). Use the grep output to populate accurately.
  - **Library upgrades** subsection — Lettuce 6.1.10→6.2+ (unlocks sharded pub/sub Track P), jnats current, Micrometer 1.9→1.10+ (Observation API unlocks RC6 deferred work), Jackson, Netty
  - **Per-feature 2.0.0 unlocks** — list what becomes possible: sharded pub/sub, Observation API trace, etc.
  - **Estimated cycle shape** — speculate: 5-8 RCs, phases (baseline migration → Lettuce-6.2 upgrade → sharded pub/sub → Observation API → final audit), top risks

- [ ] **Step 4:** Length target: 800-1200 words. Tables-heavy.

- [ ] **Step 5:** Commit: `git commit -m "docs(2.0.0): Boot 3.x compatibility matrix (Track M)"`

---

## Track G — Migration Guide DRAFT (after M completes)

**File:** `docs/2.0.0/1.9.0-to-2.0.0-migration-guide.md`

> **Implementer reads Track M's output first** to ensure the guide is accurate vs the actual surface analysis.

- [ ] **Step 1:** Read `docs/2.0.0/boot3-compatibility-matrix.md` (committed by Track M).

- [ ] **Step 2:** Write the migration guide per spec §2.3:
  - **DRAFT banner** at top
  - **Audience** (existing 1.9.0 users planning Boot 3.x upgrade)
  - **Breaking changes (expected)** — populate from Track M's findings, with concrete javax.* → jakarta.* rename examples; cite specific module impacts
  - **Non-breaking changes** — sharded pub/sub, Observation API
  - **Step-by-step upgrade** — concrete commands: version bump in POM, JDK check, sed-based mass rename if affected, run tests, sample diff
  - **New feature opt-ins** — sharded pub/sub config setup (cite Track P's proposed `pubsub-mode=sharded`), Observation API trace setup
  - **Config compatibility** — promise existing `application.yml` continues to work
  - **DRAFT banner** at bottom
  - **"Feedback welcome"** note inviting GitHub issues

- [ ] **Step 3:** Length target: 500-900 words. User-facing tone, more accessible than the design docs.

- [ ] **Step 4:** Commit: `git commit -m "docs(2.0.0): 1.9.0 → 2.0.0 migration guide DRAFT (Track G)"`

---

## Controller task: release-notes §㉓ + cut

- [ ] **Step 1:** Open `docs/release-notes-1.9.0.md`. Update header line: `RC18` → `RC19`; append `;RC19 2.0.0 prep docs（3 篇前瞻设计稿；1.9.0 自身 0 行为变更）`.

- [ ] **Step 2:** Insert §㉓ before the config-reference section:

```markdown
### ㉓ 2.0.0 prep docs / 2.0.0 prep artifacts

*Since V1.9.0-RC19.* 在 1.9.x 周期内为 **2.0.0 (Boot 3.x) cycle** 准备 3 篇前瞻设计稿（**1.9.0 自身 0 功能 / SPI / wire / config / Java 行为变更**）：

- **`docs/2.0.0/sharded-pubsub-feasibility.md`** — `RedisShardedPubSubBroker` 可行性研究：cluster-design §Capacity 的 `M·(f·N-1)` 扇出墙的 sharded pub/sub 解（Lettuce 6.2+ SSUBSCRIBE/SPUBLISH，Redis 7.0+）；fan-out 减少模型 + 自动装配选择 + 测试策略 + open questions。
- **`docs/2.0.0/boot3-compatibility-matrix.md`** — 11 模块 Boot 2.7→3.x 表面分析：JDK / Spring / 依赖版本 / javax→jakarta 量化清单 / 每模块迁移工时估算 + 2.0.0 周期形态预测。
- **`docs/2.0.0/1.9.0-to-2.0.0-migration-guide.md`** (**DRAFT**) — 用户视角迁移指南：分步骤、配置兼容性承诺、新 feature opt-in（sharded pub/sub + Observation API trace continuation）；标注 DRAFT 欢迎反馈。

#### 范围声明（务必先读）

**这 3 篇 docs 是 2.0.0 cycle 的输入，不是 1.9.0 的承诺。** 1.9.0 GA-readiness（RC17 audit 认证）未受影响；2.0.0 cycle 启动是用户驱动的下一步决定（与 1.9.0 GA cut 决策正交）。

#### 向后兼容

文档全部位于 `docs/2.0.0/`，1.9.0 代码不读这些文件。0 SPI / wire / config / 行为影响。
```

- [ ] **Step 3:** Verify test count line still says 444 (unchanged).

- [ ] **Step 4:** Commit: `git commit -m "docs(rc19): release-notes §㉓ + RC19 header"`

### Pom bump + reactor + finish

- [ ] **Step 5:** Bump POMs:

```bash
sed -i "s/1\.9\.0-RC18/1.9.0-RC19/g" pom.xml demo-netty-web-spring-boot-starter/pom.xml netty-spring-boot-autoconfigure/pom.xml netty-spring-web/pom.xml netty-spring-webmvc/pom.xml netty-spring-websocket/pom.xml netty-spring-websocket-cluster/pom.xml netty-web-spring-boot-starter/pom.xml netty-webmvc-spring-boot-starter/pom.xml netty-websocket-spring-boot-starter/pom.xml netty-websocket-cluster-spring-boot-starter/pom.xml
```

- [ ] **Step 6:** Full reactor: `mvn test` — BUILD SUCCESS, 444 tests.

- [ ] **Step 7:** Commit: `git commit -m "release: 1.9.0-RC19 - 2.0.0 prep docs (sharded pub/sub feasibility + Boot 3.x matrix + migration guide DRAFT)"`

- [ ] **Step 8:** Finish branch: FF-merge to master + tag `v1.9.0-RC19` + delete branch. STOP before push.

---

## Self-Review

- **Spec coverage:** P, M, G each produce one doc. §㉓ + header in controller task.
- **Parallel safety:** P + M file-disjoint. G sequential after M (consumes M's findings).
- **Tests delta:** 0.
- **No code change.**
